// src/components/forms/fuel-receipt-submission-form.tsx
'use client';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export function FuelReceiptSubmissionForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Fuel Receipt Submission</CardTitle>
        <CardDescription>This form is under construction.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This form will be used to submit fuel receipts for IFTA tax reporting.</p>
      </CardContent>
    </Card>
  );
}
