// src/components/forms/dispatch-communication-log-form.tsx
'use client';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export function DispatchCommunicationLogForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Dispatch Communication Log</CardTitle>
        <CardDescription>This form is under construction.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This form will be used to track communications with drivers.</p>
      </CardContent>
    </Card>
  );
}
