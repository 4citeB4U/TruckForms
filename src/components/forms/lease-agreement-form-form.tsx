// src/components/forms/lease-agreement-form.tsx
'use client';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export function LeaseAgreementForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Lease Agreement Form</CardTitle>
        <CardDescription>This form is under construction.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This form will be used to manage lease agreements.</p>
      </CardContent>
    </Card>
  );
}
