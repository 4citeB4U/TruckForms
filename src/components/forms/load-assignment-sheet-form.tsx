// src/components/forms/load-assignment-sheet-form.tsx
'use client';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export function LoadAssignmentSheetForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Load Assignment Sheet</CardTitle>
        <CardDescription>This form is under construction.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This form will be used to assign loads with pickup/delivery details.</p>
      </CardContent>
    </Card>
  );
}
