// src/components/forms/driver-qualification-file-form.tsx
'use client';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export function DriverQualificationFileForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Driver Qualification File</CardTitle>
        <CardDescription>This form is under construction.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This form will be a checklist for required driver documents.</p>
      </CardContent>
    </Card>
  );
}
