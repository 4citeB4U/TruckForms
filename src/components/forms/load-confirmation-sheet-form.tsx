// src/components/forms/load-confirmation-sheet-form.tsx
'use client';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export function LoadConfirmationSheetForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Load Confirmation Sheet</CardTitle>
        <CardDescription>This form is under construction.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This form will be used to confirm load details with shippers/brokers.</p>
      </CardContent>
    </Card>
  );
}
