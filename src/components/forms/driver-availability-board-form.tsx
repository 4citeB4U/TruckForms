// src/components/forms/driver-availability-board-form.tsx
'use client';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export function DriverAvailabilityBoardForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Driver Availability Board</CardTitle>
        <CardDescription>This form is under construction.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This form will be used to track driver availability and status.</p>
      </CardContent>
    </Card>
  );
}
